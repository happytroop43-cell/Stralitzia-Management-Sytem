name: Build Windows EXE

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Install Mesa (Software OpenGL)
      uses: f3d-app/install-mesa-windows-action@v1
      with:
        path: ${{ github.workspace }}

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install "kivy[base]" pyinstaller

    - name: Build with PyInstaller
      env:
        # Forces Kivy to use the ANGLE (DirectX) backend if standard OpenGL fails
        KIVY_GL_BACKEND: angle_sdl2
      run: |
        pyinstaller --noconfirm --onefile --windowed --name "HR_System_Windows" --hidden-import=kivy.core.window.window_sdl2 main.py

    - name: Upload Executable
      uses: actions/upload-artifact@v4
      with:
        name: HR-Program-Windows
        path: dist/HR_System_Windows.exe
        
